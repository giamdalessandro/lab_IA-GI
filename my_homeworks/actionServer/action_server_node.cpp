#include <ros/ros.h>
#include <std_msgs/Float32.h>
#include <actionlib/server/simple_action_server.h>


int main(int argc, char** argv) {
  // ros::init(argc, argv, "averaging");

  // ros::NodeHandle nh;
  // CounterServer counter(nh, "averaging");
  // ros::spin();

  return 0;
}
